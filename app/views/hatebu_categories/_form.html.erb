<%= form_for(@hatebu_category) do |f| %>
  <% if @hatebu_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hatebu_category.errors.count, "error") %> prohibited this hatebu_category from being saved:</h2>

      <ul>
      <% @hatebu_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
